$(eval $(call addlib_s,libfdtable,$(CONFIG_LIBFDTABLE)))

LIBFDTABLE_COMMON_INCLUDES-y += -I$(LIBFDTABLE_BASE)/include

CINCLUDES-$(CONFIG_LIBFDTABLE) += $(LIBFDTABLE_COMMON_INCLUDES-y)
CXXINCLUDES-$(CONFIG_LIBFDTABLE) += $(LIBFDTABLE_COMMON_INCLUDES-y)

LIBFDTABLE_SRCS-$(CONFIG_LIBFDTABLE) += $(LIBFDTABLE_BASE)/fdtable.c
